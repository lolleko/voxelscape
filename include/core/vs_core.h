#pragma once

// Those only exist for debugging purpose
// Those are not solid ways to get the "main" thread
// Technically there is no such thing as a main thread
bool debug_isMainThread();

void debug_setMainThread();